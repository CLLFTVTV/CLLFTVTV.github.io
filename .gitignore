# Hugo build output and caches
/public/
/resources/_gen/
/.hugo_build.lock

# OS / editor cruft
.DS_Store
Thumbs.db

# Local tooling config (not part of the published site)
.claude/

# Private backup repo's separate git database — must NEVER be tracked by the public repo
.backup-git/

# Raw source imports (private — never published)
import/

# NOTE: generated entries in content/ are now PUBLISHED (censored versions only).
# The raw, UNCENSORED source in import/ stays private — see the import/ rule above.
